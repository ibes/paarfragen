# Copy to .env to override in a specific environment. Never commit .env.
# Optional — src/config.ts falls back to http://127.0.0.1:8000 (script/dev-api's
# default port) when this isn't set, so it's not needed for local dev.
VITE_API_BASE_URL=http://127.0.0.1:8000
