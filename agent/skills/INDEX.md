# Skill index

No project-specific skills yet — this repo is a fresh skeleton. Reach for
the global skills already available in the session for now: `spec` to
turn an idea into a committed spec, `setup-log` after a setup decision,
`bug-report` before debugging something vague, `kaizen` to review this
agent setup itself.

Add a row here (and a `agent/skills/<name>/SKILL.md`) once a
paarfragen-specific workflow repeats often enough to be worth codifying
— don't pre-build skills for slices that don't exist yet.
