#!/usr/bin/env bash
# Description: Structural + perimeter guard — Mago's hexagonal layer boundaries (mago.toml).
# Side-effects: none (read-only).
set -euo pipefail
cd "$(dirname "$0")/.."
script/lib/mago-install
exec bin/mago guard "$@"
