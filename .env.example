# No configurable environment yet — api/ has no framework wired, frontend/
# has no API base URL to point at. Add real keys here as they're needed;
# keep this file generic (real values go in a local, gitignored .env).
